{
  "name": "openclaw-prompt-shortcuts",
  "version": "1.0.0",
  "private": true,
  "description": "OpenClaw plugin for cross-platform prompt shortcuts",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
